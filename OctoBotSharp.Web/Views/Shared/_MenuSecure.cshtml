@using OctoBotSharp.Domain

<ul class="nav navbar-nav">
    <li class="dropdown">
        <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false">Admin <span class="caret"></span></a>
        <ul class="dropdown-menu">
            @if (User.IsInRole(AuthRole.UserManagement))
            {
                <li><a href="@Url.Action("Index", "UserManagement", new { Area = "Admin" })">User Management</a></li>
            }
            
            @if (User.IsInRole(AuthRole.ItemDefinitions))
            {
                <li><a href="@Url.Action("Index", "ItemDefinitions", new { Area = "Admin" })">Item Definitions</a></li>
            }
            
            @if (User.IsInRole(AuthRole.GlobalConfig))
            {
                <li><a href="@Url.Action("Edit", "GlobalConfig", new { Area = "Admin" })">Global Config</a></li>
            }
        </ul>
    </li>
    <li><a href="@Url.Action("Index", "Members", new { Area = "" })">Members</a></li>
</ul>
